# BarkEm Configuration
# Copy to settings.yaml and customize

game:
  resolution: "1920x1080"
  display_mode: "borderless"  # Required for capture
  window_title: "THE FINALS"
  bot_embark_id: "BarkEmBot#0746"

vision:
  capture_fps: 10
  template_threshold: 0.85
  ocr_upscale_factor: 3
  debug_screenshots: true
  screenshot_dir: "./debug/screenshots"

input:
  click_delay_min: 0.1
  click_delay_max: 0.3
  drag_duration_min: 0.2
  drag_duration_max: 0.4
  typing_interval: 0.1
  humanize_movement: true

timing:
  default_cancel_timeout: 300
  match_max_duration: 1200
  state_transition_timeout: 10
  countdown_seconds: 3
  pause_cooldown: 30.0

monitoring:
  initial_poll_interval: 60
  mid_poll_interval: 30
  late_poll_interval: 10
  late_threshold_seconds: 600
  chat_poll_interval: 1.0

api:
  host: "0.0.0.0"
  port: 8080
  webhook_timeout: 30
  webhook_retries: 3

logging:
  level: "INFO"
  file: "./logs/bot.log"
  rotation: "10 MB"
  retention: "30 days"
